#pragma once
#include <string>
using namespace std;
// above are essentials for header file

// below are menu items
int menu();
void isitEvenTest(int num1);
void calcFac(int numf);
void exit();//option 3 required to exit